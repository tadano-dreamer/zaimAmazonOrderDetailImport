---
# ============================================================================
# このファイルは「横断タスク管理 manager」への報告です。
# 書くのは【このプロジェクトの Claude だけ】。manager は読むだけで、書き換えません。
# 契約の全体は manager の docs/02_アーキテクチャ.md §3。
#
# 🔴 スカラーはすべて文字列として読まれます（manager 側 D-24）。
#    タブ文字・アンカー(&x)・複数行スカラー(| >)・契約に無いキーは【エラーになって止まります】。
#    黙って無視されることはありません。困ったら空にせず、値を書いてください。
# ============================================================================

project: zaim-amazon-import      # ★必須。manager 側の id。変えない
updated_at: 2026-09-15T23:07:00+09:00   # ★必須。タイムゾーン必須。この値が古いと manager が「未報告」として拾う
mode: summary                    # ★必須。summary | issues_only | detail（下の「粒度の選び方」参照）
health: green                    # ★必須。green | yellow | red
phase: 実装と実機 Zaim への取込テストまで完了し、実データを月ごとに取り込む運用段階   # ★必須
next_action: 実データの Your Orders.zip から月ごとに CSV を出して Zaim に本番取込する   # ★必須。manager が「今日やること」に使う

# manager からの依頼に対応したら、ここに積む（manager の state/outbox/zaim-amazon-import.md を参照）
# 🔴 id だけ書いて effect を省かない。「見た」ではなく「やった」の証拠を書く（I-5）。
#    effect が照合できないと、manager は依頼を消さずに残し、次のブリーフに出します。
manager_ack:
  - { id: m-20260913-03, effect: TASKS.md をルート直下に作成しコミット }

# 未完のものだけ。完了したら消して recently_closed へ移す。
# 🔴 20件以内。全部を載せない（I-15）。載せる基準は下の「何を載せるか」。
# mode: summary のときは空リスト [] でよい。
open_tasks: []
# 1件の書き方（載せるときはこの形。★は必須）:
#   - id: zaim-amazon-import-001   # ★ <project>-NNN。採番はこのプロジェクトが持つ
#     title: タスクの内容を1行で      # ★
#     status: ready                # ★ inbox | ready | started | in_progress | blocked | waiting
#     priority: middle             # ★ max | high | middle | low（mid ではなく middle）
#     due: null                    # 2026-09-30 / 〜8月中 / 挙式日の確定前 / null
#     due_kind: none               # ★ hard | soft | approx | relative | none
#     owner: null                  # 任意
#     blocked_by: null             # 任意。この TASKS.md 内の id
#     blocked_reason: null         # 任意。status: blocked のときは書く
#     waiting_for: null            # 任意。{ who: 相手, since: 2026-09-01 }
#     done_when: null              # 任意。何が起きたら閉じてよいか
#     ref: null                    # 任意。経緯がある資料へのポインタ

# 直近7日ぶんだけ。「open_tasks から消えた＝完了」を manager に確実に伝えるため。
recently_closed: []
# 例:
# recently_closed:
#   - { id: zaim-amazon-import-000, title: 片付いた作業, closed_at: 2026-09-06 }
---

## いまの状況

ここから下は**人間用**。manager は読みません（機械可読部は上の frontmatter だけ）。

- Web アプリは GitHub Pages で公開済み（URL は `README.md`）。
- 2026-09-15 時点で単体テスト 98件が緑。Pages のデプロイは直近3回（2026-09-12）とも成功。
- 2026-09-12 にダミー6行で実機 Zaim への取込テストに成功（`docs/IMPLEMENTATION.md` §11-10）。
- **実データを Zaim へ本番取込した記録は、リポジトリにはまだ無い。** 取り込んだら `next_action` を更新する。

### `open_tasks` を空にしている理由

期限のある残作業も、他を止めているブロッカーも無い。台帳に残っている未完の事項は次のとおりで、
いずれも期限なし・他を止めていないため `TASKS.md` には載せず、元の台帳に置いたままにしている。

| 未完の事項 | 台帳 |
|---|---|
| Zaim の「品目」欄の文字数上限が未確認（安全側の60文字で運用中。52文字までは実機で通過） | `docs/IMPLEMENTATION.md` §11-7 |
| Zaim 側の記録漏れ1件（2,569円）の取込（2026-09-06 の突合時点。取り込んだかの記録なし） | `docs/IMPLEMENTATION.md` §9-5 |
| ともEPOS の Amazon 購入は別アカウント。取り込むならそのアカウントでデータのリクエストが要る | `docs/IMPLEMENTATION.md` §9-7 |
| 将来拡張（PWA 化・共有メニューから ZIP を受け取る・前回設定の記憶）。任意 | `docs/IMPLEMENTATION.md` §4 Phase 4 |
| Zaim 公式 API での直接登録の調査。スコープ外（やるなら別 PLAN） | `docs/PLAN_ITEM_MEMO_SELECTION.md` §4-1 |

期限が付いたり、どれかが他の作業を止めるようになったら `open_tasks` に移し、`mode` を見直す。

> 注意: `docs/PLAN.md` 冒頭の「GitHub Pages 公開と実機 UAT のみ未」は 2026-07-18 時点の古い記述。
> 公開も実機での確認も済んでいるので、ここからタスクを起こさないこと。

---

# 📋 書き方（このファイルをコピーしたら、ここから下は消してよい）

## 置き場所とコミット

- **リポジトリのルート直下に `TASKS.md`**（確定）
- **コミットしていない報告は、存在しないのと同じ。** 変更したら必ずコミット・プッシュする

## セッションの作法（このプロジェクトの `CLAUDE.md` に1節入れる）

文面は manager の `templates/PROJECT_CLAUDE_SECTION.md` にあります。要点:

1. セッション開始時に manager の `state/outbox/<project>.md` を読む。**読めなければ黙って飛ばす**
2. 依頼に対応したら `manager_ack` に `{ id, effect }` で積む
3. タスクに変化があったら `TASKS.md` を更新し、`updated_at` を今の時刻にしてコミットする

## 粒度の選び方（`mode`）

| mode | 使いどころ | `open_tasks` |
|---|---|---|
| `summary` | 動いていない／個票を出す意味がない（例: 受験が終わった学習プロジェクト） | **空でよい。** `phase` と `next_action` だけで報告する |
| `issues_only` | 残作業が数件で、一覧できる（例: 小さなツール） | 未完のものを全部 |
| `detail` | 台帳が別にあって、期限や依存が絡む（例: 結婚） | **絞って載せる**（次項） |

## 何を載せるか — 🔴 全部載せない

**`mode: detail` で載せるのは次の2つだけ。**

1. **期限が90日以内のもの**
2. **ブロッカー**（これが片付かないと他が進まないもの）

残りは**元の台帳に置いたままにする。** `TASKS.md` は台帳の**エクスポート**であって、移設ではありません。

> **理由**: 「消すには重要すぎるが、進めるには手強すぎるタスク」が積み上がると、
> ダッシュボードを開いた瞬間に読む気が失せ、**仕組みごと使われなくなります**（manager の `docs/04` I-15）。
> 247件を見て放棄した実例が報告されています。**20件以内**を守ってください。

## `due` と `due_kind`

`due` は**書いたまま**が manager に渡ります（日付型に変換されません）。
どう扱ってほしいかは `due_kind` で伝えます。

| `due_kind` | 意味 | `due` の例 | manager の扱い |
|---|---|---|---|
| `hard` | 動かせない | `2026-09-30` | 期限どおりに強く鳴らす |
| `soft` | 目標。少しずれてよい | `2026-09-30` | 弱めに扱う |
| `approx` | おおよそ | `〜8月中` / `2027-06-10ごろ` | さらに弱く。毎朝は鳴らさない |
| `relative` | 他の出来事が基準 | `挙式日の確定前` | **鳴らさない。人が見る** |
| `none` | 期限なし | `null` | 期限では並べない |

🔴 **`due_kind` を全部 `hard` にしないでください。** ゆるい期限を `hard` にすると、
毎朝そのタスクが最上位に出続け、**ダッシュボードが信用されなくなります。**

## 記号を値に入れたいとき

`[` `]` `{` `}` は**フロー形式の記号**として読まれます。値に含めたいときは引用符で囲んでください。
` #`（空白＋シャープ）以降は行末コメントとして落ちます。こちらも引用符で囲めば残ります。

```yaml
title: "[保留] 帝国ホテルへの連絡"     # ← 角括弧は引用符が要る
title: 進捗100#達成                   # ← 空白が無いのでコメントにならない（そのまま値）
title: "見積もり # 2回目"              # ← ` #` を値に含めたいときも引用符
```

## 迷ったときの既定値

`status: ready` / `priority: middle` / `due: null` / `due_kind: none` / 任意項目は `null`。

## 書けないことがあったら

**無理に押し込まず、manager に言ってください。** 契約に無いキーを足すと
**パースが止まります**（黙って無視されるより、止まるほうが安全という設計です）。
足りない属性は manager 側の契約を直して対応します。
